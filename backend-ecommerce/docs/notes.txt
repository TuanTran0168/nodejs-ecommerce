
# Init project (create package.json)
$ npm init -y

# Check Node version
$ node -v

# Install Express
$ npm install express --save

# Start project
$ node server.js
$ npm run start

@@ Install important package @@

# Install Morgan (show log when users request to server)
$ npm install morgan --save

@ Install Helmet (protect private infomation)
$ npm install helmet --save-dev

$ curl http://localhost:3000 --include

@ Install Compression (compress response bodies for all requests)
$ npm install compression --save-dev

# Install Mongoose
$ npm install mongoose --save-dev

# Install dotenv
$ npm install dotenv --save-dev

--- Install Extension: Mongo Snippets for Node-js
--- Install Extension: httpYac - Rest Client

# Install Bcrypt
$ npm install bcrypt --save

# Install Crypto
$ npm install crypto --save

# Install JsonWebToken (JWT)
$ npm install jsonwebtoken --save

# Install Lodash (like DTO in Java Spring)
$ npm install lodash --save